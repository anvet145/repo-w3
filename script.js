let functionSum = (a, b) => {console.log(a + b)};
functionSum(1,2 );

const infoPerson = {
    firstName: "defunger",
    lastName : "vann", 
    age: 25,
};

var functionInfo = (infoPerson) => {return infoPerson.firstName + " " + infoPerson.lastName};
console.log(functionInfo(infoPerson));